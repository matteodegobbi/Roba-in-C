#include <stdio.h>
/*CONQUER DELLA MAX RICORSIVA*/
int max2numeri(int a,int b){
    return (a>=b)?a:b;
}
int maxRicorsiva(int* arr){
    printf("%d",arr[0]);
}
int main(int argc, char const *argv[]){
    /*printf("%d \n",max2numeri(atoi(argv[1]),atoi(argv[2])));*/

    return 0;
}
